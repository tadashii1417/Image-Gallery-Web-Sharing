import React from 'react';
import styles from './ResultWelcome.module.css';

export default function (props) {
    return (
        <div className={styles.container}>
            <h4>Category pages</h4>
            This is all images in category pages.
        </div>
    );
}